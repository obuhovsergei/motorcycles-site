import React from 'react'

export const Loader = () => (
    <div className="progress" style={{display:'flex', justifyContext:'center', paddingTop:'2rem'}}>
        <div className="indeterminate"/>
    </div>
)